#ifndef ADC_CONTROL_H
#define ADC_CONTROL_H

#include "stm32f1xx_hal.h"
#include "cmsis_os.h"

/* I2C ?????? ADS1115 (ADDR ? GND) */
#define ADS1115_ADDR         (0x48 << 1)

/* ???????? */
#define ADS1115_REG_CONVERT  0x00
#define ADS1115_REG_CONFIG   0x01

/* PGA — ???????? ??????? ??????? */
typedef enum {
    ADS1115_PGA_6144 = 0x0000,  /* ±6.144? */
    ADS1115_PGA_4096 = 0x0200,  /* ±4.096?  ? ????????????? */
    ADS1115_PGA_2048 = 0x0400,  /* ±2.048? */
    ADS1115_PGA_1024 = 0x0600,  /* ±1.024? */
    ADS1115_PGA_0512 = 0x0800,  /* ±0.512? */
    ADS1115_PGA_0256 = 0x0A00,  /* ±0.256? */
} ADS1115_PGA;

/* ??????? ????????????? */
typedef enum {
    ADS1115_SPS_8   = 0x0000,
    ADS1115_SPS_16  = 0x0020,
    ADS1115_SPS_32  = 0x0040,
    ADS1115_SPS_64  = 0x0060,
    ADS1115_SPS_128 = 0x0080,  /* ? ?? ????????????? */
    ADS1115_SPS_250 = 0x00A0,
    ADS1115_SPS_475 = 0x00C0,
    ADS1115_SPS_860 = 0x00E0,
} ADS1115_SPS;

/* ????????? ????? ??? */
typedef struct {
    I2C_HandleTypeDef  *hi2c;
    /* UART_HandleTypeDef *huart; ? ??????? ??? ????? */
    ADS1115_PGA         pga;
    ADS1115_SPS         sps;
    int16_t             channels[4];
    float               voltage[4];
    uint8_t             initialized;
} ADC_Handle;

/* ????????? ????????? — ????????? ? ????? ?????? */
extern ADC_Handle hadc_ext;

/* ??????? */
void ADC_Init(ADC_Handle *h,
              I2C_HandleTypeDef *hi2c,
              ADS1115_PGA pga,
              ADS1115_SPS sps);

int16_t ADC_ReadChannel(ADC_Handle *h, uint8_t channel);
void    ADC_ReadAllChannels(ADC_Handle *h);
float   ADC_ToVoltage(ADC_Handle *h, int16_t raw);
void    ADC_SendPacket(ADC_Handle *h);

/* FreeRTOS ?????? — ???????????? ? freertos.c */
void    ADC_Task(void *argument);

#endif /* ADC_CONTROL_H */